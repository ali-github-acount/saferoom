import 'package:flutter/material.dart';

class SRErrorBox extends StatelessWidget {
  const SRErrorBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(child: const Text("ERORR")),
    );
  }
}
